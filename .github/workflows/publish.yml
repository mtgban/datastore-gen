name: publish

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 7 * * *"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install golang
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build the datastore
        run: |
          go run . -o riftbound.json
          # Consumers decompress by suffix (simplecloud.InitReader), so the
          # published object keeps the .xz extension.
          xz -9 riftbound.json
          ls -l riftbound.json.xz

      - name: Keep a copy on the run
        uses: actions/upload-artifact@v4
        with:
          name: riftbound.json.xz
          path: riftbound.json.xz
          retention-days: 7

      - name: Publish to B2
        env:
          B2KEY: "${{ secrets.B2_APPLICATION_KEY_ID_DATASTORE }}"
          B2SECRET: "${{ secrets.B2_APPLICATION_KEY_DATASTORE }}"
        run: |
          pipx install b2
          b2 account authorize "$B2KEY" "$B2SECRET"
          b2 file upload --no-progress "${{ vars.B2_BUCKET }}" riftbound.json.xz riftbound/riftbound.json.xz
          b2 account clear
